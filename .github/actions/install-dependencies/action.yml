name: 'Install Dependencies'
description: 'Upgrade pip and install requirements'
runs:
  using: 'composite'
  steps:
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
      shell: bash